# Arran benchmark mode

Arran is a benchmark dataset, not an inference model. DRIFT therefore treats it as a real benchmark execution mode: a mission supplies an Arran dataset and a prediction directory, and the benchmark adapter validates the supplied benchmark assets and records reproducible scoring inputs. It must not be presented as a detector.
